## **Status:**
- Review: Approved
- PR: Todo

## Metadata
- **Title:** Tạo Flutter project + cấu hình folder structure
- **Phase:** Phase 1 — Project Setup
- **GitHub Issue:** #9

---

## Description
Khởi tạo Flutter project mới cho Ymusic (iOS-only) và cấu hình cấu trúc thư mục theo spec.

- Tạo Flutter project với bundle ID phù hợp
- Xóa platform không cần thiết (Android, Web, Windows, Linux, macOS) — chỉ giữ iOS
- Tạo folder structure theo `lib/` spec: `core/`, `data/`, `presentation/`, `router/`
- Tạo các subfolder: `constants/`, `theme/`, `utils/`, `models/`, `repositories/`, `services/`, `screens/`, `widgets/`, `providers/`
- Tạo file placeholder `README.md` hoặc `.gitkeep` trong mỗi folder rỗng

---

## Design
- File: N/A (không có wireframe cho task này)
- Màn hình: N/A

---

## Acceptance Criteria
- [ ] Flutter project chạy được trên iOS simulator
- [ ] Chỉ có platform `ios/` trong project root
- [ ] Cấu trúc thư mục `lib/` khớp với spec trong CLAUDE.md section 7
- [ ] `flutter analyze` không có error

---

## Implementation Checklist
- [ ] Tạo Flutter project: `flutter create --org com.ymusic ymusic --platforms ios`
- [ ] Xóa các platform thừa nếu cần
- [ ] Tạo folder structure theo spec
- [ ] Thêm `.gitkeep` vào các folder rỗng
- [ ] `flutter analyze` — 0 warnings

---

## Notes
- Bundle ID gợi ý: `com.ymusic.dev`
- Xem cấu trúc thư mục chi tiết ở CLAUDE.md section 7
- Task này không có dependency, là bước đầu tiên của toàn project

## Screenshots

| Screen Name | Navigation Steps | Expected State |
|---|---|---|
| app-launch | Run app on iOS simulator | Default Flutter counter app hoặc blank screen, không crash |
