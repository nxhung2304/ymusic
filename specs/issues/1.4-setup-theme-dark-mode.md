## **Status:**
- Review: Approved
- PR: Draft

## Metadata
- **Title:** Setup ThemeData (dark / light mode) + MaterialApp
- **Phase:** Phase 1 — Project Setup
- **GitHub Issue:** (to be filled after sync)

---

## Description
Cấu hình dark theme cho toàn app và setup `MaterialApp` trong `app.dart`. Hỗ trợ chuyển sang light mode

- Tạo `AppTheme` với `ThemeData` dark mode dùng `AppColors` và `AppTypography`
- Cấu hình `MaterialApp` / `MaterialApp.router` trong `app.dart`
- Cập nhật `main.dart` để gọi `app.dart`

---

## Design
- File: N/A
- Màn hình: N/A (áp dụng cho toàn app)

---

## Acceptance Criteria
- [ ] App khởi động với dark background (`AppColors.background`)
- [ ] `ThemeData` dùng `AppColors`, `AppTypography` — không có màu hardcode
- [ ] `MaterialApp` được tách ra file `app.dart` riêng, `main.dart` chỉ gọi `runApp`
- [ ] `flutter analyze` — 0 errors

---

## Implementation Checklist
- [ ] Tạo `lib/core/theme/app_theme.dart` — `AppTheme.dark` trả về `ThemeData`
- [ ] Cấu hình `colorScheme`, `scaffoldBackgroundColor`, `appBarTheme`, `textTheme` trong ThemeData
- [ ] Tạo `lib/app.dart` — `class YMusicApp extends StatelessWidget`
- [ ] `app.dart` dùng `MaterialApp` (chưa dùng router), set `theme: AppTheme.dark`
- [ ] Cập nhật `lib/main.dart`: `runApp(const YMusicApp())`
- [ ] `flutter analyze` — 0 warnings

---

## Notes
- Depends on: **1.3**
App dùng dark / light mode
- Dark mode là mặc định  — set `themeMode: ThemeMode.dark`
- `go_router` sẽ được thêm ở Phase 6 khi có màn hình thực để navigate
- Giai đoạn này `MaterialApp` có thể dùng `home: Scaffold(body: Center(child: Text('YMusic')))` làm placeholder

## Screenshots

| Screen Name | Navigation Steps | Expected State |
|---|---|---|
| app-launch | Run app on iOS simulator | Dark background, placeholder text "YMusic" ở giữa màn hình |
