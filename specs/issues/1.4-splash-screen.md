## **Status:**
- Review: Pending
- PR: Todo

## Metadata
- **Title:** SplashScreen (session check → redirect router)
- **Phase:** Phase 1 – Authentication & Routing
- **GitHub Issue:** (to be filled after sync)

---

## Description
Màn hình khởi động app — kiểm tra auth session và redirect về đúng màn hình.

- Hiển thị logo/branding trong khi app khởi động
- Lắng nghe `authStateProvider` để xác định user đã đăng nhập chưa
- Nếu có session → redirect về `/home`
- Nếu chưa đăng nhập → redirect về `/login`
- Transition mượt (fade/slide) sang màn hình đích

---

## Design
- File: `specs/designs/ymusic_design.pen`
- Màn hình: Splash Screen

---

## Acceptance Criteria
- [ ] Logo app hiển thị khi khởi động
- [ ] Nếu user đã đăng nhập → tự động vào Home (không qua Login)
- [ ] Nếu chưa đăng nhập → redirect về Login
- [ ] Không bị stuck ở Splash nếu Firebase chậm khởi tạo
- [ ] Transition animation sang màn hình đích
- [ ] `flutter analyze` — 0 warnings

---

## Implementation Checklist
- [ ] Tạo `lib/features/auth/presentation/screens/splash_screen.dart`
- [ ] Hiển thị logo/branding UI
- [ ] Dùng `authStateProvider` (StreamProvider) — listen first emit để quyết định redirect
- [ ] Implement timeout fallback (ví dụ: sau 5s nếu không có emit → redirect /login)
- [ ] Thêm const constructors
- [ ] flutter analyze — 0 warnings

---

## Notes
- Splash chỉ là "loading gate" — không navigate thủ công, để go_router redirect handling làm việc này (xem 1.5)
- Hoặc: Splash widget tự navigate sau khi xác định auth state — tùy approach chọn trong 1.5
- Depends on: 1.1 (AuthDatasource), 1.2 (authProvider)
- Required by: 1.5 (go_router — `/` route trỏ về SplashScreen)

## Screenshots
| Screen Name | Navigation Steps | Expected State |
|---|---|---|
| splash | Cold app launch | Logo visible, no crash, redirects within 2s |
