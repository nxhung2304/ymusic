## **Status:**
- Review: Approved
- PR: Merged

## Metadata
- **Title:** Tạo constants (AppColors, AppTypography, AppSpacing, AppEndpoints)
- **Phase:** Phase 1 — Project Setup
- **GitHub Issue:** #15

---

## Description
Tạo các file constants dùng xuyên suốt toàn app. Tất cả màu sắc, typography, spacing, và API endpoints đều được define tại đây — không hardcode trong widget.

- Tạo `AppColors` với bảng màu dark theme
- Tạo `AppTypography` với text styles
- Tạo `AppSpacing` với các giá trị spacing cố định
- Tạo `AppEndpoints` với YouTube Data API base URL và các endpoint

---

## Design
- File: N/A
- Màn hình: N/A

---

## Acceptance Criteria
- [ ] `AppColors` có đầy đủ màu cho dark theme (background, surface, primary, onPrimary, text, subtext, error)
- [ ] `AppTypography` define các text style: h1, h2, h3, body, caption, label
- [ ] `AppSpacing` define các giá trị: xs, sm, md, lg, xl, xxl
- [ ] `AppEndpoints` có YouTube Data API v3 base URL
- [ ] Tất cả constants đều là `static const`
- [ ] `flutter analyze` — 0 errors

---

## Implementation Checklist
- [ ] Tạo `lib/core/constants/app_colors.dart` — AppColors class
- [ ] Tạo `lib/core/constants/app_typography.dart` — AppTypography class
- [ ] Tạo `lib/core/constants/app_spacing.dart` — AppSpacing class
- [ ] Tạo `lib/core/constants/app_endpoints.dart` — AppEndpoints class
- [ ] Tạo `lib/core/constants/constants.dart` — barrel export file
- [ ] `flutter analyze` — 0 warnings

---

## Notes
- Depends on: **1.1**
- Màu sắc tham khảo YouTube Music dark theme: background `#0F0F0F`, surface `#1A1A1A`, primary `#FF0000` (hoặc tùy chỉnh)
- Font: dùng system default (SF Pro trên iOS), không import custom font ở giai đoạn này
- `AppEndpoints.youtubeSearch` = `https://www.googleapis.com/youtube/v3/search`
- API key lưu ở env/config riêng — KHÔNG hardcode vào constants

## Screenshots

| Screen Name | Navigation Steps | Expected State |
|---|---|---|
| N/A | N/A | Không có UI — chỉ verify qua flutter analyze |
