## **Status:**
- Review: Approved
- PR: Merged

## Metadata
- **Title:** Config Firebase (GoogleService-Info.plist, firebase_options.dart)
- **Phase:** Phase 1 — Project Setup
- **GitHub Issue:** #25

---

## Description
Tích hợp Firebase vào Flutter iOS project. Thêm các package Firebase cần thiết, cấu hình file config, và khởi tạo Firebase trong `main.dart`.

- Thêm `firebase_core` + `firebase_auth` + `cloud_firestore` vào `pubspec.yaml`
- Thêm `GoogleService-Info.plist` vào iOS project
- Tạo `firebase_options.dart` bằng FlutterFire CLI
- Khởi tạo `Firebase.initializeApp()` trong `main.dart`

---

## Design
- File: N/A
- Màn hình: N/A

---

## Acceptance Criteria
- [ ] `firebase_core`, `firebase_auth`, `cloud_firestore` đã được thêm vào `pubspec.yaml`
- [ ] `GoogleService-Info.plist` đã có trong `ios/Runner/`
- [ ] `firebase_options.dart` đã được tạo bởi FlutterFire CLI
- [ ] `Firebase.initializeApp()` được gọi trước `runApp()` trong `main.dart`
- [ ] App build và chạy được trên iOS simulator — không crash
- [ ] `flutter analyze` — 0 errors

---

## Implementation Checklist
- [ ] Thêm packages vào `pubspec.yaml`: `firebase_core`, `firebase_auth`, `cloud_firestore`
- [ ] Chạy `flutter pub get`
- [ ] Tạo Firebase project trên Firebase Console (nếu chưa có)
- [ ] Đăng ký iOS app với bundle ID `com.ymusic.dev`
- [ ] Download và đặt `GoogleService-Info.plist` vào `ios/Runner/`
- [ ] Chạy FlutterFire CLI: `flutterfire configure` → tạo `lib/firebase_options.dart`
- [ ] Cập nhật `main.dart`: thêm `await Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform)`
- [ ] `main()` phải là `async` và gọi `WidgetsFlutterBinding.ensureInitialized()` trước Firebase init
- [ ] Build và run trên iOS simulator — verify không crash
- [ ] `flutter analyze` — 0 warnings

---

## Notes
- Depends on: **1.1**
- `firebase_options.dart` có thể commit (không chứa secret key nguy hiểm) nhưng cẩn thận nếu có API key
- Firestore Security Rules sẽ được viết ở task **3.6** — giai đoạn này dùng default rules tạm
- Cần cài FlutterFire CLI: `dart pub global activate flutterfire_cli`
- Tham khảo: https://firebase.google.com/docs/flutter/setup

## Screenshots

| Screen Name | Navigation Steps | Expected State |
|---|---|---|
| app-launch | Run app on iOS simulator | App khởi động bình thường, không có Firebase error trong console |
